import numpy as np 

def DFT(InputVektor):
    N = len(InputVektor)
    j = 0
    x_j= 0
    LsgVektor = np.zeros(N)
    for i in range(N)
        LsgVektor[i]=InputVektor[i]*exp(-2*3.141*np.imag(1)*i)
